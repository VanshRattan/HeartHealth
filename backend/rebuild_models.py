df.drop(['id', 'gender', 'Residence_type'], inplace=True, axis=1)
def outlier_removal(column, df):
"""
Rebuild script removed per user request.
If you need to regenerate the build workflow later, request it and I'll re-add the script.
"""
